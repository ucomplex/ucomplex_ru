# 🚀 Быстрый деплой на Cloud Run через веб-консоль

Так как gcloud CLI не установлен, используем веб-консоль Google Cloud.

---

## Шаг 1: Закоммитьте и запушьте изменения

```bash
# В терминале выполните:
git add .
git commit -m "Fix: Simplify .dockerignore to include PDF files"
git push origin main
```

---

## Шаг 2: Настройте Cloud Build триггер (однократно)

### 2.1 Откройте Cloud Build Triggers

Откройте в браузере:
```
https://console.cloud.google.com/cloud-build/triggers
```

### 2.2 Создайте новый триггер (если его ещё нет)

1. Нажмите **"CREATE TRIGGER"** или **"СОЗДАТЬ ТРИГГЕР"**

2. Заполните поля:
   - **Name**: `deploy-ucomplex-ru`
   - **Region**: `global` или `us-central1`
   - **Event**: `Push to a branch` (Push в ветку)
   - **Source**: 
     - Repository: подключите ваш GitHub репозиторий `ucomplex.ru`
     - Branch: `^main$` или `^master$`
   - **Configuration**:
     - Type: `Cloud Build configuration file`
     - Location: `Repository` 
     - Cloud Build configuration file: `/cloudbuild.yaml`
   
3. Нажмите **"CREATE"** (Создать)

### 2.3 Включите необходимые API

Откройте и включите эти API (если попросит):
```
https://console.cloud.google.com/apis/library/cloudbuild.googleapis.com
https://console.cloud.google.com/apis/library/run.googleapis.com
https://console.cloud.google.com/apis/library/containerregistry.googleapis.com
```

---

## Шаг 3: Запустите сборку

### Вариант A: Автоматически (через push)

Просто сделайте push в main ветку:
```bash
git push origin main
```

### Вариант B: Вручную (через консоль)

1. Откройте Cloud Build:
   ```
   https://console.cloud.google.com/cloud-build/builds
   ```

2. Нажмите **"RUN TRIGGER"** и выберите ваш триггер

3. Или нажмите **"SUBMIT BUILD"** и выберите:
   - Source: `Repository`
   - Branch: `main`
   - Configuration: `cloudbuild.yaml`

---

## Шаг 4: Отслеживайте сборку

1. Откройте страницу сборок:
   ```
   https://console.cloud.google.com/cloud-build/builds
   ```

2. Вы увидите новую сборку со статусом:
   - 🔵 **Working** - сборка идёт (5-10 минут)
   - ✅ **Success** - успешно собрано и задеплоено
   - ❌ **Failed** - ошибка (откройте логи)

3. Нажмите на сборку чтобы увидеть логи в реальном времени

---

## Шаг 5: Проверьте Cloud Run сервис

1. Откройте Cloud Run:
   ```
   https://console.cloud.google.com/run
   ```

2. Найдите сервис `ucomplex-ru` в списке

3. Нажмите на него - вы увидите:
   - 🌐 **URL** сервиса (вверху страницы)
   - 📊 **Metrics** (метрики)
   - 📝 **Logs** (логи)
   - ⚙️ **Revisions** (версии)

4. Скопируйте URL (будет вида `https://ucomplex-ru-xxxxx-uc.a.run.app`)

---

## Шаг 6: Проверьте работу

### Проверьте главную страницу:
```
https://ваш-url.run.app
```

### Проверьте PDF файлы:
```
https://ваш-url.run.app/ucdocs/inn.pdf
https://ваш-url.run.app/ucdocs/license.pdf
https://ваш-url.run.app/ucdocs/ogrn.pdf
https://ваш-url.run.app/ucdocs/ustav.pdf
```

Если PDF открываются - всё работает! ✅

---

## 🚨 Если что-то пошло не так

### Сборка Failed (провалилась)

1. Откройте логи сборки в Cloud Build
2. Найдите строку с ошибкой (обычно красным)
3. Частые проблемы:
   - **Git LFS**: убедитесь что в логах есть `✅ LFS файлы загружены`
   - **Docker build failed**: проверьте синтаксис Dockerfile
   - **Permission denied**: проверьте права Cloud Build Service Account

### Сервис не отвечает (502/503)

1. Откройте Cloud Run сервис
2. Нажмите **LOGS** (Логи)
3. Ищите ошибки:
   - `Error: Cannot find module` - проблема со standalone сборкой
   - `EADDRINUSE` - проблема с портом
   - `Out of memory` - не хватает памяти

4. Увеличьте память если нужно:
   - Откройте сервис → **EDIT & DEPLOY NEW REVISION**
   - **Container** → **Resources**
   - Memory: `512 MiB` или `1 GiB`

### PDF файлы недоступны (404)

1. Проверьте логи сборки - видны ли файлы:
   ```
   📄 Проверяем наличие PDF файлов:
   -rw-r--r-- 1 root root 15234 Jan 21 10:30 inn.pdf
   ```

2. Если файлов нет в логах - проблема с Git LFS или .dockerignore

3. Проверьте локально:
   ```bash
   ls -lh public/ucdocs/
   ```
   
   Если файлы меньше 1 KB - это LFS указатели, нужно:
   ```bash
   brew install git-lfs  # или apt-get install git-lfs
   git lfs install
   git lfs pull
   git add .
   git commit -m "Update LFS files"
   git push
   ```

---

## 📝 После успешного деплоя

### Настройте кастомный домен

См. файл `DOMAIN-SETUP.md` или в веб-консоли:

1. Откройте ваш Cloud Run сервис
2. Перейдите на вкладку **MANAGE CUSTOM DOMAINS**
3. Нажмите **ADD MAPPING**
4. Введите `ucomplex.ru`
5. Добавьте DNS записи у регистратора

---

## 🔧 Полезные ссылки

- **Cloud Build**: https://console.cloud.google.com/cloud-build/builds
- **Cloud Run**: https://console.cloud.google.com/run
- **Логи**: https://console.cloud.google.com/logs
- **IAM**: https://console.cloud.google.com/iam-admin/iam
- **Биллинг**: https://console.cloud.google.com/billing

---

## ✅ Чеклист успешного деплоя

- [ ] Код запушен в GitHub (main ветка)
- [ ] Cloud Build триггер настроен
- [ ] Сборка успешна (Success)
- [ ] Сервис виден в Cloud Run
- [ ] Главная страница открывается
- [ ] PDF файлы доступны
- [ ] Настроен кастомный домен (опционально)

---

**Вопросы?** Проверьте `DEPLOY.md` и `UCDOCS-FIX.md` для подробностей!
