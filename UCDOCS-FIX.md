# 📄 Исправление проблемы с ucdocs PDF файлами

## Проблема

После деплоя через GitHub PDF файлы из папки `public/ucdocs/` не отображаются на сайте.

## Причина

PDF файлы хранятся в **Git LFS** (Large File Storage), и при сборке Docker образа копируются только указатели LFS вместо реальных файлов.

---

## 🔍 Диагностика

Запустите скрипт диагностики:

```bash
chmod +x fix-ucdocs.sh
./fix-ucdocs.sh
```

Или проверьте вручную:

```bash
# Проверьте размер PDF файлов
ls -lh public/ucdocs/

# Если файлы меньше 1KB - это LFS указатели!
# Посмотрите содержимое:
head public/ucdocs/inn.pdf

# Должно быть PDF содержимое, а не:
# version https://git-lfs.github.com/spec/v1
```

---

## ✅ Решение

### Вариант 1: Автоматическое исправление (Рекомендуется)

Я уже обновил файлы для автоматического исправления:

1. **Обновлен `cloudbuild.yaml`** - добавлен правильный порядок загрузки LFS файлов
2. **Обновлен `Dockerfile`** - добавлены проверки наличия PDF
3. **Обновлен `.dockerignore`** - явно разрешено копирование `public/` директории

**Что нужно сделать:**

```bash
# 1. Убедитесь, что Git LFS установлен
git lfs version

# Если не установлен:
# macOS:
brew install git-lfs

# Linux:
sudo apt-get install git-lfs

# Windows:
# Скачайте с https://git-lfs.github.com/

# 2. Инициализируйте Git LFS
git lfs install

# 3. Подтяните настоящие файлы
git lfs pull

# 4. Проверьте размер файлов (должно быть > 10KB)
ls -lh public/ucdocs/

# 5. Закоммитьте обновленные конфигурации
git add cloudbuild.yaml Dockerfile .dockerignore
git commit -m "Fix: Ensure PDF files are included in Docker build"

# 6. Запушьте изменения - Cloud Build автоматически пересоберет
git push
```

После push'а Cloud Build автоматически:
- Подтянет LFS файлы
- Соберет Docker образ с правильными PDF
- Задеплоит новую версию

---

### Вариант 2: Коммит реальных файлов (Альтернатива)

Если хотите отказаться от Git LFS для этих файлов:

```bash
# 1. Удалите файлы из LFS
git lfs untrack "*.pdf"

# 2. Удалите правило из .gitattributes
# Отредактируйте .gitattributes и удалите строку:
# *.pdf filter=lfs diff=lfs merge=lfs -text

# 3. Добавьте файлы как обычные
git rm --cached public/ucdocs/*.pdf
git add public/ucdocs/*.pdf

# 4. Закоммитьте
git commit -m "Store PDF files directly in Git"
git push
```

**Плюсы:** Проще, не нужен LFS  
**Минусы:** Увеличивает размер репозитория

---

### Вариант 3: Ручной апплоад файлов в контейнер

Если нужно срочно исправить на работающем сервисе:

```bash
# Получите имя контейнера из Cloud Run
gcloud run services describe ucomplex-ru --region=us-central1

# Вариант A: Пересоберите с --build-arg
gcloud builds submit --config cloudbuild.yaml .

# Вариант B: Используйте Cloud Storage для статики
# 1. Загрузите файлы в Cloud Storage
gsutil mb gs://ucomplex-ru-static
gsutil -m cp public/ucdocs/*.pdf gs://ucomplex-ru-static/ucdocs/

# 2. Сделайте bucket публичным
gsutil iam ch allUsers:objectViewer gs://ucomplex-ru-static

# 3. Обновите ссылки в коде на:
# https://storage.googleapis.com/ucomplex-ru-static/ucdocs/filename.pdf
```

---

## 🔄 Проверка после исправления

После деплоя проверьте:

```bash
# 1. Проверьте что сервис обновился
gcloud run services describe ucomplex-ru --region=us-central1 --format='value(status.latestCreatedRevisionName)'

# 2. Проверьте доступность PDF файлов
curl -I https://YOUR-SERVICE-URL/ucdocs/inn.pdf

# Должно вернуть:
# HTTP/2 200
# content-type: application/pdf
# content-length: [размер больше 10000]

# 3. Откройте в браузере
open https://YOUR-SERVICE-URL/ucdocs/inn.pdf
```

---

## 📊 Мониторинг логов сборки

Просмотрите логи Cloud Build чтобы убедиться что файлы копируются:

```bash
# Последняя сборка
gcloud builds list --limit=1

# Логи конкретной сборки
gcloud builds log BUILD_ID

# Или в веб-консоли
# https://console.cloud.google.com/cloud-build/builds
```

В логах вы должны увидеть:

```
✅ LFS файлы загружены:
-rw-r--r--  1 root root  15234 Jan 21 10:30 inn.pdf
-rw-r--r--  1 root root  23456 Jan 21 10:30 license.pdf
...

📄 Проверяем наличие PDF файлов:
-rw-r--r--  1 root root  15234 Jan 21 10:30 inn.pdf
...

📄 Проверяем PDF файлы в финальном образе:
-rw-r--r--  1 nobody nogroup  15234 Jan 21 10:30 inn.pdf
```

---

## 🚨 Возможные проблемы

### Проблема: "This repository is over its data quota"

**Решение:**
```bash
# Git LFS имеет квоту на GitHub (1GB бесплатно)
# Проверьте использование:
git lfs ls-files -s

# Опции:
# 1. Купите больше LFS хранилища на GitHub
# 2. Используйте Вариант 2 (храните файлы напрямую)
# 3. Используйте Cloud Storage (Вариант 3)
```

### Проблема: Файлы все еще не копируются

**Решение:**
```bash
# Очистите кеш Docker
gcloud builds submit --config cloudbuild.yaml . --no-cache

# Или добавьте в cloudbuild.yaml флаг --no-cache (уже добавлен)
```

### Проблема: "command not found: git-lfs"

**Решение:**
```bash
# Установите Git LFS
# macOS:
brew install git-lfs

# Ubuntu/Debian:
curl -s https://packagecloud.io/install/repositories/github/git-lfs/script.deb.sh | sudo bash
sudo apt-get install git-lfs

# После установки:
git lfs install
```

---

## 📝 Обновленные файлы

Я обновил следующие файлы для автоматического решения проблемы:

### `cloudbuild.yaml`
- ✅ Добавлено логирование LFS файлов
- ✅ Добавлен флаг `--no-cache` для свежей сборки

### `Dockerfile`
- ✅ Добавлены проверки наличия PDF на этапе builder
- ✅ Добавлены проверки в финальном образе

### `.dockerignore`
- ✅ Явно разрешено копирование `public/` директории
- ✅ Явно разрешены `*.pdf` файлы

---

## 🎯 Быстрое решение (TL;DR)

```bash
# 1. Установите Git LFS
brew install git-lfs  # или apt-get install git-lfs

# 2. Подтяните файлы
git lfs install
git lfs pull

# 3. Проверьте файлы
ls -lh public/ucdocs/

# 4. Пушните обновления
git add .
git commit -m "Fix: Ensure PDF files are included"
git push

# 5. Подождите ~5-10 минут пока Cloud Build пересоберет
# 6. Проверьте: https://ваш-сайт.run.app/ucdocs/inn.pdf
```

---

## 💡 Рекомендации на будущее

1. **Используйте Cloud Storage для больших файлов** - это дешевле и быстрее
2. **Мониторьте квоты Git LFS** - могут закончиться
3. **Добавьте тесты** в CI/CD для проверки наличия файлов
4. **Используйте CDN** для доставки статических файлов

```javascript
// Пример для Next.js - использование Cloud Storage
const PDF_BASE_URL = process.env.NEXT_PUBLIC_STORAGE_URL || '/ucdocs';

export function getPdfUrl(filename) {
  return `${PDF_BASE_URL}/${filename}`;
}
```

---

## 📞 Нужна помощь?

- [Git LFS документация](https://git-lfs.github.com/)
- [Cloud Build документация](https://cloud.google.com/build/docs)
- [Docker .dockerignore](https://docs.docker.com/engine/reference/builder/#dockerignore-file)
